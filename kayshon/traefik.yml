http:
  middlewares:
    method-whitelist-plugin:
      plugin:
        method-whitelist:
          Message: "Method Not Allowed"
          Methods:
            - GET
  routers:
    leafwiki-router:
      entryPoints:
        - "websecure"
      middlewares:
        - method-whitelist-plugin
      rule: "Host(`www.bob.house`)"
      service: "leafwiki-service"
      tls: "true"
    miasma-router:
      entryPoints:
        - "websecure"
      rule: "Host(`miasma.bob.house`)"
      service: "miasma-service"
      tls: "true"
  services:
    leafwiki-service:
      loadBalancer:
        servers:
        - url: "http://leafwiki:8080"
    miasma-service:
      loadBalancer:
        servers:
        - url: "http://miasma:9999"
